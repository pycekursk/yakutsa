@model Vk

<div class="row">
    <h4 class="text-center">@ViewData["Title"]</h4>
</div>

<div class="row">
    <div class="col-sm-10 mx-auto">
        @{
            Html.RenderPartial("/Views/Partials/FormPartial.cshtml", (object)Model);
        }
    </div>
</div>
<div class="row">
    <div class="col-sm-10 mx-auto">
        <input type="button" value="AsyncDebug" onclick="PromiseTest()" />
    </div>
</div>
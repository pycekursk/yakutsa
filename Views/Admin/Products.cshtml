@model List<object>

<div class="row">
    <h4 class="text-center">@ViewData["Title"]</h4>
</div>

<div class="row">
    <div class="col-sm-12">
        @Html.Partial("/Views/Partials/TablePartial.cshtml", (List<object>)Model)
    </div>
</div>